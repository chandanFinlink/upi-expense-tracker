import React from "react";
import PlaceholderScreen from "@/screens/PlaceholderScreen";

export default function BudgetsScreen() {
  return (
    <PlaceholderScreen
      title="Budgets"
      note="Per-month, per-category budgets — wired up in Step 7 (fixes original month-scoping bug)."
    />
  );
}
