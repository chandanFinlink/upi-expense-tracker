import React from "react";
import PlaceholderScreen from "@/screens/PlaceholderScreen";

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      note="SMS permission card (Android only), clear data, currency — wired up in Step 9."
    />
  );
}
