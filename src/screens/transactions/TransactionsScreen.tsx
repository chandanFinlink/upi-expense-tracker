import React from "react";
import PlaceholderScreen from "@/screens/PlaceholderScreen";

export default function TransactionsScreen() {
  return (
    <PlaceholderScreen
      title="Transactions"
      note="List, search & filter chips — wired up in Step 5."
    />
  );
}
